{
  "name": "midi-captain-max-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright end-to-end tests for the home page (site/), driving the real firmware logic in the MicroPython wasm engine.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
