# Development dependencies for CI and local development
# Install with: pip install -r requirements-dev.txt

# Linting
# Upper bound is deliberate: ruff's default rule selection changes between
# minor releases (0.16 widened it), and an unpinned floor let CI install a
# ruff that failed the lint gate on code nobody had touched. pyproject.toml
# now names the rule set explicitly, so this bound is belt-and-braces —
# bump it when adopting a new minor, not implicitly at the next CI run.
ruff>=0.16.3,<0.17

# Testing with hardware mocks
pytest>=8.0.0

# JSON Schema validation for config tests
jsonschema>=4.0.0

# Optional: Adafruit Blinka for more realistic hardware simulation
# adafruit-blinka>=8.0.0
